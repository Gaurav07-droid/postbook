extends baseEmail

block Content
    p Hi #{firstname},
    p Forgot your password ? Submit the token sent to your email to reset password.
    p Yout token: #{url}
    table.btn.btn-primary(role="presentation", border="0", cellpadding="0", cellspacing="0")
        tbody
            tr
                td(align="left")
                    table(role="presentation", border="0", cellpadding="0", cellspacing="0")
                        tbody
                            tr
                                td
                                    a(href=`${url}`, target="_blank") Reset your password
    p If you didn't forgot yor password,please ignore this email!
